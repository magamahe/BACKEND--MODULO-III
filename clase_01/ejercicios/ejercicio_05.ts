export {}; // archivo independiente
// Declaramos una variable que puede ser string o null
let miDato: string | null = "Hola, TypeScript!";

// Imprimimos su valor inicial
console.log("Valor inicial:", miDato);

// Cambiamos el valor a null
miDato = null;

// Imprimimos su nuevo valor
console.log("Valor después de asignar null:", miDato);
