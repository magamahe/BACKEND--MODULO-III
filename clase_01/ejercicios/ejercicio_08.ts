// ejercicio_08.ts
export {}; // Evita conflictos de variables globales

// Declaramos dos variables numéricas
let numero1: number = 15;
let numero2: number = 7;

// Realizamos una operación matemática simple (suma)
let resultado: number = numero1 + numero2;

// Imprimimos el resultado
console.log(`${numero1} + ${numero2} = ${resultado}`);
