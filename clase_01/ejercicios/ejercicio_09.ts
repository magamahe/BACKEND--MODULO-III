// ejercicio_09.ts
export {}; // Evita conflictos de variables globales

// Declaramos dos variables booleanas
let esEstudiante: boolean = true;
let tieneAcceso: boolean = false;

// Operaciones lógicas
console.log("esEstudiante AND tieneAcceso:", esEstudiante && tieneAcceso);
console.log("esEstudiante OR tieneAcceso:", esEstudiante || tieneAcceso);
console.log("NOT esEstudiante:", !esEstudiante);
console.log("NOT tieneAcceso:", !tieneAcceso);
