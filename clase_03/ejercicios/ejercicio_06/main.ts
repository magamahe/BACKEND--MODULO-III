// main.ts
import { Geometry } from './geometry';

console.log('Área del círculo (r=5):', Geometry.areaOfCircle(5)); // 78.53981633974483
console.log('Área del cuadrado (lado=4):', Geometry.areaOfSquare(4)); // 16
